/*
let pass="          "
pass.padEnd(8,"?")
console.log(pass);


result = "pass is:".padEnd(15, "?");
console.log(result);

numCard="1234567891234567".slice(0,12).padEnd(16,"*")
 console.log(numCard);
 */
/*
let para = prompt("bad?");
let censor = "this is bad and awful very much".includes(para);
if ((censor = true)) {
  censor = "this is bad and awful very much".indexOf(para);
  parametr = para.length;
  subsan = parametr + censor;
  ncensor = "this is bad and awful very much".substring(subsan, censor);
  console.log(parametr);
  console.log(para);
  console.log(subsan);
  console.log(ncensor);
  lorem = "this is bad and awful very much".slice(0, censor);
  loreml = "this is bad and awful very much".length;
  edame = "this is bad and awful very much".slice(subsan, loreml);
  console.log(lorem);
  console.log(edame);
  console.log(`${lorem}${edame}`);
  
}
*/
